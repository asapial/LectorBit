# `src/components/` — shared UI primitives

Project-wide visual primitives: buttons, dialogs, tooltips, virtualized list wrappers, progress bars. Feature-specific UI lives in `src/features/<x>/components/`.

Once shadcn/ui is wired, shadcn-generated components also live here.
