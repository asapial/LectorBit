{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability. Lists ONLY the narrow permissions LectorBit actually uses.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "lectorbit:default"
  ]
}
